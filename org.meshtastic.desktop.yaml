id: org.meshtastic.desktop
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
sdk-extensions:
  # OpenJDK 17 and 21 are required at build time.
  - org.freedesktop.Sdk.Extension.openjdk17
  - org.freedesktop.Sdk.Extension.openjdk21
command: meshtastic-wrapper.sh

finish-args:
  - --share=ipc
  # Allow the app to communicate with the X server
  - --socket=x11
  # Enable Hardware Accel
  - --device=dri
  # Required for USB Serial support
  - --device=all
  # Needs network access
  - --share=network
  # Status tray / notifications support
  - --talk-name=org.kde.StatusNotifierWatcher
  - --talk-name=org.freedesktop.Notifications
  # Allow bluetooth if available
  - --allow=bluetooth
  - --system-talk-name=org.bluez.*
  # Set env for the bundled JBR
  - --env=PATH=/app/jre/bin:/app/bin:/usr/bin
  - --env=JAVA_HOME=/app/jre

modules:
  # Jetbrains Runtime (JBR) JRE must be used for proper window scaling, font rendering, etc.
  - name: jbr
    buildsystem: simple
    build-commands:
      - mkdir -p /app/jre
      - tar xzf jbr-*.tar.gz -C /app/jre --strip-components=1
    sources:
      - type: file
        url: https://cache-redirector.jetbrains.com/intellij-jbr/jbr-21.0.11-linux-x64-b1163.116.tar.gz
        sha256: 72bf2d1830587a3cd31b718d4af9fc7a88fc6c3740642d842b3b378d4acbe476
        only-arches:
          - x86_64
      - type: file
        url: https://cache-redirector.jetbrains.com/intellij-jbr/jbr-21.0.11-linux-aarch64-b1163.116.tar.gz
        sha256: 02124d01591c173f9a51ef84d99ce248900d4d1e1aa52a38c65f4708f66b6e98
        only-arches:
          - aarch64
  - shared-modules/libappindicator/libappindicator-gtk3-12.10.json

  - name: meshtastic-wrapper
    buildsystem: simple
    build-commands:
      - install -Dm755 meshtastic-wrapper.sh /app/bin/meshtastic-wrapper.sh
    sources:
      - type: file
        path: meshtastic-wrapper.sh

  - name: meshtastic-desktop
    buildsystem: simple
    build-options:
      append-path: "/usr/lib/sdk/openjdk21/bin"
      # build-args:
      #   # Allow internet access at build time (Not allowed on FlatHub)
      #   - --share=network
      env:
        JAVA_HOME: /usr/lib/sdk/openjdk21/jvm/openjdk-21
    build-commands:
      # Use local gradle zip
      - sed -i 's|distributionUrl=.*|distributionUrl=gradle-all.zip|' gradle/wrapper/gradle-wrapper.properties
      # Install desktop metadata
      - install -Dm644 desktopApp/packaging/icons/icon.svg /app/share/icons/hicolor/scalable/apps/org.meshtastic.desktop.svg
      - install -Dm644 -t /app/share/applications desktopApp/packaging/linux/org.meshtastic.desktop.desktop
      - desktop-file-edit --set-key="Exec" --set-value="meshtastic-wrapper.sh %U"
        /app/share/applications/org.meshtastic.desktop.desktop
      - install -Dm644 -t /app/share/metainfo desktopApp/packaging/linux/org.meshtastic.desktop.metainfo.xml
      # Disable Gradle's auto-download and auto-detect of Java installations
      - echo "org.gradle.java.installations.auto-detect=false" >> gradle.properties
      - echo "org.gradle.java.installations.auto-download=false" >> gradle.properties
      - echo "org.gradle.java.installations.paths=/usr/lib/sdk/openjdk21/jvm/openjdk-21,/usr/lib/sdk/openjdk17/jvm/openjdk-17" >> gradle.properties
      # Comment out the toolchainVendor=JETBRAINS line in the Gradle daemon JVM properties
      # as it causes Gradle to fail when running with OpenJDK
      - >
        sed -i
        's/^\(\s*\)vendor\.set(JvmVendorSpec\.JETBRAINS)/\1\/\/ vendor.set(JvmVendorSpec.JETBRAINS)/'
        desktopApp/build.gradle.kts
      # Build!
      - ./gradlew --offline :desktopApp:packageUberJarForCurrentOS
      - >
        JAR_FILE=$(find desktopApp/build/compose/jars/ -name "*.jar" -type f | head -1)
        && install -Dm755 "$JAR_FILE" /app/lib/meshtastic-desktop.jar
    sources:
      - type: git
        url: https://github.com/meshtastic/Meshtastic-Android.git
        tag: v2.7.14-open.14
        commit: f2ac9ef10f7bef5050663701b65d618a13b2cc88
        x-checker-data:
          is-main-source: true
          type: json
          url: https://api.github.com/repos/meshtastic/Meshtastic-Android/releases/latest
          timestamp-query: .published_at
          tag-query: .tag_name
          version-query: $tag | sub("^v"; "")
      - type: patch
        paths:
          - patches/remove-toolchainManagement.patch
      - type: file
        url: https://services.gradle.org/distributions/gradle-9.5.1-all.zip
        sha256: c72fb9991f6025cbe337d52ba77e531b3faf62bdd3e348fe1ccee9f51c71adb0
        dest: "gradle/wrapper"
        dest-filename: "gradle-all.zip"
        x-checker-data:
          type: json
          url: https://services.gradle.org/versions/current
          is-main-source: false
          version-query: .version
          url-query: .downloadUrl
      - flatpak-sources.json
